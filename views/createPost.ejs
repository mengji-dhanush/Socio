<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Socio-Create New Post</title>
    <link rel="stylesheet" href="/css/create-new-post.css">
    <link rel="stylesheet" href="/css/style.css">
  </head>
  <body>
    <div class="nav">
      <a href="/posts">
        <div class="inner-nav-b">
          <img src="/utilities/SocioLogo.png" alt="Logo" class="logo" />
          <h3>Socio</h3>
        </div>
      </a>
      <div class="inner-nav-a">
        <form action="/profile/edit" method="get">
          <button>
            <img
              src="<%= currUser.profilePhoto.url %>"
              alt="Profile Picture"
              class="profile-pic"
            />
          </button>
        </form>
        <form action="/posts/new"><button>Create New Post</button></form>
        <form action="/logout">
          <button>Logout</button>
        </form>
      </div>
    </div>
    <% if(success && success.length){ %>
      <div class="success">
        <h3><%=success%></h3>
      <% } %>
      <% if(error && error.length){ %>
        <div class="error">
          <h3><%=error%></h3>
        <% } %>
    <div class="container">
      <div class="form-container">
        <h1>Create New Post</h1>
        <form method="POST" action="/posts/new" enctype="multipart/form-data">
          <div class="form-group">
            <label for="image-upload">Upload Image:</label><br><br>
            <input type="file" name="postImage" id="image-upload" required>
            <label for="content">Content:</label><br><br>
            <textarea name="content" id="content" required rows="5" cols="50"></textarea>
            <button>Create</button>
          </div>
  </body>
</html>
