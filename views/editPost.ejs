<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Post</title>
    <link rel="stylesheet" href="/css/edit-post.css" />
  </head>
  <body>
    <% if(success && success.length){ %>
      <div class="success">
        <h3><%=success%></h3>
      <% } %>
      <% if(error && error.length){ %>
        <div class="error">
          <h3><%=error%></h3>
        <% } %>
    <div class="container">
      <h1>Edit Post</h1>
      <form action="/posts/edit/<%= post._id %>?_method=PUT" method="POST" >
        <div class="form-group">
          <label for="content">Content:</label><br /><br />
          <textarea name="post[content]" rows="10" required><%= post.content %></textarea>
        </div>
        <div class="form-group">
          <input type="file" />
        </div>
        <div class="form-group">
          <button type="submit">Update Post</button>
        </div>
      </form>
    </div>
  </body>
</html>
